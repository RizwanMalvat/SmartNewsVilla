package com.smartnewsvillaadmin.utilities;

public class CreateExcel {
//
//    public static Workbook Create(List<String[]> BodyValue, String[] headerExcel) {
//
//        // Using XSSF for xlsx format, for xls use HSSF
//        Workbook workbook = new XSSFWorkbook();
//        //new HSSFWorkbook().getBytes();
//
////        Sheet TimeSheet = workbook.createSheet("Fans");
////        Row header = TimeSheet.createRow(0);
////        CellStyle cellStyle = workbook.createCellStyle();
////        cellStyle.setAlignment(CellStyle.ALIGN_CENTER_SELECTION);
////
////        header.setRowStyle(cellStyle);
////        for (int i = 0; i < headerExcel.length; i++) {
////            header.createCell(i).setCellValue(headerExcel[i]);
////        }
////        for (int i = 0; i < BodyValue.size(); i++) {
////            Row row = TimeSheet.createRow(i + 1);
////            for (int j = 0; j < BodyValue.get(i).length; j++) {
////                row.createCell(j).setCellValue(BodyValue.get(i)[j]);
////            }
////        }
//
//        //write this workbook in excel file.
////        try {
////            String filePath = "C:\\excels\\" + new Date();
////            FileOutputStream fos = new FileOutputStream("C:\\excels");
////            workbook.write(fos);
////            fos.close();
////
////        } catch (FileNotFoundException e) {
////            e.printStackTrace();
////        } catch (IOException e) {
////            e.printStackTrace();
////        }
//        return workbook;
//    }
}
